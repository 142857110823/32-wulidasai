# AGENTS.

本文件用于承接“将 `AGENTS.md` 与 `Agent.md` 合并”的当前工作要求。

## Source Status

- `AGENTS.md`：存在，已读取并作为主规则来源。
- `Agent.md`：当前工作区不存在，无法进行字面内容合并。

因此，本文件在现阶段采用如下策略：

1. 完整继承现有 `AGENTS.md` 的有效规则。
2. 显式记录 `Agent.md` 缺失这一事实。
3. 不伪造不存在的 `Agent.md` 内容。

---

# AGENTS.md

默认使用中文。
本工作区只服务于 `FreshSalt Surface / 表面盐影像助手`。

## 1. 启动顺序

- 先检查当前目录、已有文件、规则文件、可运行命令。
- 再读取 `AGENTS.md`、`rules/freshsalt.rules.md`、`docs/source/baseline_2026-06-14/` 下三份基准文件。
- 新任务默认先调用并遵守 `using-superpowers` 与 `freshsalt-platform-priority-guard`。
- 如果任务涉及 skill 编写或修改，再额外使用 `skill-creator`。
- 优先级顺序：用户原话 > 用户给出的方案/框架/rules/AGENTS/基准文档 > 技能 > 个人习惯。

## 2. 项目身份

这是一个面向大学物理竞赛的 Android 优先 Flutter APP 原型，核心围绕：

- 受控 RGB 成像
- ROI 与灰卡质控
- 半物理 / 灰箱模型
- 模拟数据验证
- 后续真实实验接入

## 3. 基准文件

后续工作默认以下文件为基线，不替换、不混入其他项目：

- `docs/source/baseline_2026-06-14/大学物理竞赛-app.txt`
- `docs/source/baseline_2026-06-14/FreshSalt_Surface_APP框架决策树_输入交互模型输出_2026-06-13.md`
- `docs/source/baseline_2026-06-14/FreshSalt_Surface_APP平台方案与点击式模拟验证规划_2026-06-12.docx`

## 4. 核心边界

- 不混入 `water_erosion_mvp` 或其他竞赛平台内容。
- 不输出食品安全或执法性质结论。
- 不把模型公式、图像处理逻辑直接写进 UI 页面。
- 不伪造验证结果；未运行就明确写未运行。
- 所有模拟数据必须持续显示“模拟数据”标记。
- 所有结论必须基于文件、代码、命令输出、测试结果或用户资料。

## 5. 平台优先规则

- 浏览器可见结果优先于代码结构自洽。
- 首页和主平台骨架未形成完整平台观感前，不优先补测试，不无脑扩模块，不用 `flutter analyze` / `flutter test` 自我说服。
- 每次只修一个最高优先级可见偏差。
- 不把“路由存在”“页面存在”“组件存在”当作已完成的前端结果。
- 先看基准图与浏览器现状差异，再决定下一步。

## 6. 工作方式

- 先读后改。
- 简单优先。
- 小步修改。
- 直接完成能完成的工作，不空谈方案。
- Bug 先定位根因，再做最小修复。
- Review 先报问题，按严重程度排序，并带文件和行号。

## 7. 记录要求

- 每次关键进展都更新 `workflow.log`。
- `workflow.log` 从现在开始默认只用英文记录。
- `workflow.log` 仅保留最近 7 天内且不超过 30 条记录，超出时滚动删除最旧内容。
- 任何小瑕疵、偏差、阻塞，先记录到 `outputs/workflow-error-2026-06-15.md`，再继续执行。

## 8. 方案输出默认格式

以后如需输出方案、计划或下一步安排，默认按以下 5 段输出：

- 当前进度
- 待完成工作
- 执行步骤
- 验证标准
- 验收条件
